"""reproflow: a reproducible-engineering framework for security-relevant
data pipelines.

Public API:
    reproflow.provenance  -- record what was fetched, when, and from where
    reproflow.gate        -- scan a package for draft stamps / placeholders / secrets
    reproflow.audit       -- score a repository against the reproducibility checklist
    reproflow.scaffold    -- generate a new project from the standard layout
"""

__version__ = "0.1.0"
